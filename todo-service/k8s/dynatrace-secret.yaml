apiVersion: v1
kind: Secret
metadata:
  name: dynakube
  namespace: dynatrace
type: Opaque
stringData:
  # Values are injected by setup.sh via kubectl create secret --from-literal
  # Do NOT commit real tokens here — this file is a reference only.
  apiToken: "${DT_API_TOKEN}"